record Point(int x, int y);

/* Función para mover
   un punto en el eje X */
void move(Point p) {
    if (p.x >= 10 && p.y < 5) {
        p.x = p.x + 1;
    } else {
        return;
    }
}
// comentario de una línea

if (!(a == 0) || b == 3){
    break;
}
